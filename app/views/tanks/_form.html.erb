<%= form_for(tank) do |f| %>
  <% if tank.errors.any? %>
    <div class="card">
      <div class="card-header bg-danger text-white">
        <%= pluralize(tank.errors.count, "error") %> prohibited this tank from being saved:
      </div>
      <div class="card-body">
        <ul>
        <% tank.errors.full_messages.each do |message| %>
          <li class="card-text"><%= message %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <form>
    <div class="form-group">
      <%= f.label :name %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
  
    <div class="actions">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </form>
<% end %>
